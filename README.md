# Simple-Greeting-Function-Project
This project contains a simple Python function for generating personalized greeting messages. The greet() function takes a person's name as input and returns a customized greeting string.
